<main class="flex-1 flex items-start justify-center min-h-[calc(100vh-200px)] px-2 sm:px-4 py-6 sm:py-8">
  <div
    class="w-full max-w-full sm:max-w-5xl lg:max-w-7xl xl:max-w-screen-xl mx-auto
           bg-slate-900/70 backdrop-blur-md
           rounded-2xl shadow-xl border border-slate-700/70
           p-4 sm:p-6 md:p-10 text-white"
  >

    <!-- Header -->
    <header class="mb-8">
      <p class="text-xs uppercase tracking-widest text-slate-400 mb-2">
        Retiros
      </p>
      <h1 class="text-2xl sm:text-3xl font-extrabold tracking-tight">
        Confirmar retiro
      </h1>
      <p class="text-sm text-slate-300 mt-1 max-w-xl">
        Revisá los datos del retiro autorizado y confirmá para registrar el movimiento.
      </p>
    </header>

    <!-- Card principal -->
    <div class="flex justify-center">
      <div
        class="w-full max-w-md bg-slate-800/80
               border border-slate-600/70 rounded-2xl
               p-6 sm:p-8 shadow-lg"
      >

        <div class="space-y-4">
          <div class="rounded-xl border border-slate-600/70 bg-slate-900/60 p-4">
            <p class="text-xs uppercase tracking-widest text-slate-400 mb-1">
              Importe autorizado
            </p>
            <p class="text-2xl sm:text-3xl font-extrabold text-emerald-300 break-words">
              <%= montoFormateado %>
            </p>
          </div>

          <div class="rounded-xl border border-slate-600/70 bg-slate-900/60 p-4">
            <p class="text-xs uppercase tracking-widest text-slate-400 mb-1">
              Autorizado por
            </p>
            <p class="text-sm sm:text-base text-slate-200 font-semibold">
              <%= adminNombre %>
            </p>
          </div>
        </div>

        <form action="/retiros/confirmar-retiro" method="POST" class="mt-6">
          <input type="hidden" name="codigo" value="<%= codigoAutorizacion %>">

          <button
            type="submit"
            class="w-full bg-emerald-600 hover:bg-emerald-700 active:bg-emerald-800
                   text-white font-semibold py-3 rounded-xl
                   shadow-md hover:shadow-lg transition"
          >
            Confirmar retiro
          </button>

          <p class="text-xs text-slate-400 mt-3 text-center">
            Al confirmar, se registrará el retiro en el historial del cobrador.
          </p>
        </form>

      </div>
    </div>

  </div>
</main>
